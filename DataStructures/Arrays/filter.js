const numbers = [10, 5, 20, 15, 25];

const filteredNumbers = numbers.filter((number) => number > 10);

console.log(filteredNumbers); // Output: [20, 15, 25]

// The filter method creates a new array with all elements that pass the provided test function.
